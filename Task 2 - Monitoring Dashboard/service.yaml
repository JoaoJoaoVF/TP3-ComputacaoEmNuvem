apiVersion: v1
kind: Service
metadata:
  name: dashboard-joaoferreira
  namespace: joaoferreira
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30421
  selector:
    app: dashboard
